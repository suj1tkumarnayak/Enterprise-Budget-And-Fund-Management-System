/**
 * Global hooks — shared across all features.
 *
 * Feature-specific hooks live in their feature folder:
 *   frontend/src/features/<feature>/hooks/
 */
export { useApiError } from './useApiError';
